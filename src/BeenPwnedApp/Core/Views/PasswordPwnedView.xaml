<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" BackgroundColor="#b94242"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="BeenPwned.App.Core.Views.PasswordPwnedView" Margin="0,-6,0,0">
    <ContentView.Content>
        <StackLayout VerticalOptions="Center" Padding="20,0,20,40" Spacing="10">
            <Label Text="&#x0001F629;" FontSize="100" VerticalOptions="Center" HorizontalOptions="Center"/>
            <Label FontSize="20" TextColor="White" Style="{StaticResource BoldLabel}" Text="Oh no.." HorizontalTextAlignment="Center" />
            <Label FontSize="15" TextColor="White" Text="This password appears in the database, which means it has been compromised. If you are still using this password (you probably should not be entering it in apps like these..) you should change it immediately." HorizontalTextAlignment="Center" />
        </StackLayout>
    </ContentView.Content>
</ContentView>